{
  "name": "aframe.io",
  "private": true,
  "version": "0.0.0",
  "description": "A-Frame",
  "repository": "aframevr/aframe.io",
  "author": "A-Frame",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/aframevr/aframe.io/issues"
  },
  "homepage": "https://aframe.io/",
  "devDependencies": {
    "autoprefixer": "^6.0.3",
    "bole": "^2.0.0",
    "ecstatic": "^1.4.0",
    "garnish": "^5.0.1",
    "ghpages": "0.0.3",
    "http-server": "^0.8.5",
    "postcss-cli": "^2.3.2",
    "shelljs": "^0.5.3",
    "term-color": "^1.0.1"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "node ./scripts/server",
    "build": "rm -rf _build && mkdir -p _build/css/ && postcss --use autoprefixer css/style.css -d _build/css/ --browsers 'last 10 versions' --map --no-cascade",
    "prod": "npm run build && rm -rf _prod && mkdir -p _prod && cp -r {.nojekyll,CNAME,css,images,fonts,js,*.ico,*.html,*.xml,*.json} _prod/. 2>/dev/null || : && cp -r _build/* _prod/. && npm run spa",
    "spa": "node ./scripts/spa",
    "preghpages": "npm run prod",
    "ghpages": "ghpages",
    "gh-pages": "npm run ghpages",
    "publish": "npm run ghpages",
    "deploy": "npm run ghpages"
  }
}
